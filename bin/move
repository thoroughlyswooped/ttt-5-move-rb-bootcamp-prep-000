#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
puts "Where would you like to make your next move"
user_input = get.strip;
index = input_to_index(user_input);
board = [ " ", 
